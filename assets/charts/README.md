# Chart Assets

The current charts are generated from data constants in `script.js` so the displayed values stay
easy to audit. Add static SVG exports here only if a future publication page needs reusable chart
files.
