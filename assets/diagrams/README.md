# Diagram Assets

The current project page renders its main diagrams as semantic HTML/CSS figures in `index.html`
and `styles.css`. Add exported SVG versions here only if the site later needs standalone image
assets for sharing, previews, or reuse.
